spring:
  application:
    name: ledger
  web:
    resources:
      add-mappings: false
  mvc:
    throw-exception-if-no-handler-found: true

server:
  error:
    include-stacktrace: never
    include-message: never
